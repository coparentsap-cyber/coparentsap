#!/usr/bin/env bash
set -e
echo "🔧 pre-push (rapide) — checks non bloquants"
if npm run | grep -q "^  lint$"; then npm run lint || true; fi
if npm run | grep -q "^  format$"; then npm run format || true; fi
if npm run | grep -q "^  build$"; then npm run build || true; fi
echo "✅ OK: push autorisé."
